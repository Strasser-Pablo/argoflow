apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: abbcompression-mysql
  namespace: abbcompressionheg
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
