apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: abbcompression-mysql
  namespace: abbcompressionheg
spec:
  StorageClass: gluster-heketi-distributed 
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
